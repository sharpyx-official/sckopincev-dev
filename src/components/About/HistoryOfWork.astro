---
import type { WorkItem } from '@data/HistoryOfWorkService'
import HistoryOfWorkItem from './HistoryOfWorkItem.astro'

interface Props {
  works: WorkItem[]
}

const { works } = Astro.props
---

<div class="self-center">
  {works.map(work => <HistoryOfWorkItem work={work} />)}
</div>
